Steps need to be followed before running the python file

Need to install Pandas module 
pip install pandas

Extract the python file and input csv file

Run the following command in terminal
python covidsurvey.py

